package model.tutoring;

public class Student extends User{
    public Student(String userName, String password, String fullName, String major, int academicYear) {
        super(userName, password, fullName, major, academicYear);
    }
}
